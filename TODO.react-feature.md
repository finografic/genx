# TODO: react feature

## VSCode / formatter

- Add oxc/oxfmt formatter settings for `typescriptreact`, `javascriptreact`, `html` languages when `react` (or similar) is a dependency
- HTML: include `[html]` in oxfmt/VSCode settings here, or keep separate from css feature?
- css feature adds `css.validate: false`, `scss.validate: false`, oxfmt for css/scss
- The **oxfmt** feature’s language categories already add tsx/jsx/css/scss/html when `react` is a dep — align React feature with that or extend
