NPM_TOKEN=

# AI commit drafts for `genx managed status`. Values here take precedence over the
# shell environment, so this file decides the model even when ~/.zshrc exports one.
# Leave blank to auto-select from MODEL_PREFERENCE in src/lib/ai/commit-message.ts.
OLLAMA_DEFAULT_MODEL=
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_KEEP_ALIVE=30m
